name: Docker sync

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4


    - name: docker build bot
      run: |
        docker build . -t migueel15/discordipbot:latest
